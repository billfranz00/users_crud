extends base.pug

block content
	h2 Add a new user here:
	form(action="/users" method="POST")
		input(type="text" name="name" placeholder="Enter your name...")
		button(type="submit") Submit it dude...